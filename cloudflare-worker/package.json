{
  "name": "traffic-tile-server",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker: serves PMTiles road segments as {z}/{x}/{y}.mvt tiles",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "pmtiles": "^4.4.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "^5.4.0",
    "wrangler": "^4.0.0"
  }
}
