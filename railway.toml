[build]
builder        = "DOCKERFILE"
dockerfilePath = "Dockerfile.fetcher"

[deploy]
# This is a one-shot batch job, not a web service.
# Never restart after the script finishes.
restartPolicyType = "NEVER"
# No healthcheck — this is not an HTTP server.
healthcheckPath = ""
